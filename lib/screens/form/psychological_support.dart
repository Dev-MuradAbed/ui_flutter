import 'package:flutter/material.dart';

class PsychologicalSupport extends StatelessWidget {
  const PsychologicalSupport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PsychologicalSupport"),),
    );
  }
}
